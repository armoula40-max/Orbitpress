# انسخه إلى .env في جذر المستودع بجانب docker-compose.yml — إلزامي:
# رمز الدخول إلى واجهة OrbitPress (كلمة السر الخاصة بك)
ORBITPRESS_TOKEN=change-me-to-a-long-random-token

# اختياري:
# ORBITPRESS_PORT=8080
# ORBITPRESS_SECRET_KEY=  (64 hex chars — يُولّد ويُحفظ تلقائياً داخل volume)

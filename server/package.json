{
  "name": "orbitpress-server",
  "version": "4.0.0",
  "description": "OrbitPress server edition — WordPress content studio with a built-in social scraper and FeedSpy-style analyzers.",
  "main": "index.js",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test test/*.test.js",
    "install-browser": "playwright install chromium"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "playwright": "^1.49.1"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
